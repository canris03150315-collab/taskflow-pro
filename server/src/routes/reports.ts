import express from 'express';

const router = express.Router();

// TODO: 實作報表相關路由
// - GET /api/reports/tasks - 任務報表
// - GET /api/reports/attendance - 出勤報表  
// - GET /api/reports/performance - 績效報表

export { router as reportRoutes };
